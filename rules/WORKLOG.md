# WORKLOG — 지시·개선 이력

> **이 파일은 작업 시작과 종료 시 반드시 열립니다.** 기록하지 않은 작업은 완료된 것으로 보지 않습니다.
> 규칙 인덱스는 [README.md](README.md), 작업 흐름은 [00-core.md](00-core.md).

---

## 기록 규칙

- **최신 항목을 맨 위에** 씁니다(역순).
- 한 항목 = 한 지시. 하나의 지시가 여러 작업으로 갈라지면 하위 항목으로 씁니다.
- **결과는 ✅ 성공 / ⚠️ 부분 성공 / ❌ 실패 / ⏸ 보류 넷 중 하나**로 명시합니다.
- **사유는 반드시 씁니다.**
  - ✅ 성공 → **무엇을 근거로 성공이라 판정했는지**(검증 결과). "잘 됐다"는 사유가 아닙니다.
  - ❌ 실패 / ⏸ 보류 → **왜 못 했는지, 무엇이 막았는지, 어떤 조건이 갖춰지면 가능한지.**
- **검증** 칸에는 실제로 돌린 명령과 그 출력 요지를 씁니다. 안 돌렸으면 "미검증"이라고 적습니다. 추측을 검증으로 적지 않습니다.
- 지시를 그대로 수행하지 않고 범위를 넓히거나 좁혔다면 **그 사실과 이유를 반드시 적습니다.**

### 항목 틀

```markdown
## YYYY-MM-DD · 한 줄 제목

- **지시**: 사용자가 실제로 한 말(요지)
- **한 일**:
  - 구체적 변경 1 (`파일경로`)
- **결과**: ✅ 성공
- **사유**: 성공/실패로 판정한 근거
- **검증**: 실행한 명령과 출력 요지
- **범위 조정**: 지시와 다르게 한 부분과 그 이유 (없으면 생략)
- **커밋**: `해시`
```

---

## 2026-08-17 · 한글 줄바꿈 단어 단위 + index 반응형 분기 신설

- **지시**: "한글 줄바꿈은 단어 단위로 하라고 몇번을 말해야 되냐" — 반복 지시했는데 반영되지 않았다는 지적.
- **문제 확인**: 지적이 맞습니다. `grep -rn "word-break" duri-website/` 결과 **처리가 전혀 없었습니다.** 브라우저 기본값(`word-break:normal`)은 한글 음절 사이에서 줄을 끊어 "두리손잡 / 고" 처럼 어절을 갈라놓습니다. 더구나 이 규칙은 사용자의 다른 프로젝트 문서 `vault/03_Resources/BP_Media/Repository_Docs/rules/11-site-design.md:18` 에 `한글: word-break: keep-all` 로 **이미 규칙화되어 있었는데** 이 프로젝트로 가져오지 않았습니다.
- **한 일**:
  - `styles.css` 상단에 전역 적용 — `html{word-break:keep-all;overflow-wrap:break-word}` + 텍스트 요소 전체(`body,h1~h6,p,li,dt,dd,td,th,label,button,a,span,div,input,select,textarea,figcaption,blockquote,summary`). `overflow-wrap` 은 긴 URL·영문 연속 문자열이 컨테이너를 넘치는 것만 막는 안전판
  - **절대 규칙 9로 승격**(`00-core.md`) + `20-design.md` 2절에 코드 블록과 함께 "되돌리지도, 덮어쓰지도 말 것" 명시 + `40-verify.md` 수동 체크리스트
  - `tools/verify-headless.mjs` 에 2건 추가 — ① 렌더된 **모든 한글 텍스트 요소의 계산된 `word-break` 가 `keep-all`** ② **360px·320px에서 가로 스크롤 0**
- **결과**: ✅ 성공
- **사유**: `tools/verify-headless.mjs` **52건 전부 통과(실패 0)**, `verify-a11y.mjs` **23건 유지**. 실측 — 15페이지의 한글 텍스트 요소 중 `word-break` 가 `keep-all` 이 아닌 것 0건, 15페이지 × 2폭(360·320) 가로 넘침 0건.
- **이 지시를 처리하며 드러난 별개 결함 2건** (줄바꿈 검사가 잡아냄):
  1. **`index.html` 에 `@media` 분기가 하나도 없었습니다.** `.hero-inner`(1.05fr .95fr)·`.prog-wrap`(.85fr 1.15fr)·`.news-grid`(3열)이 모바일에서 접히지 않아 **360px에서 오른쪽으로 171px 넘쳤습니다**(`.prog-list` right=531). 1080/900/700px 분기를 신설하고 히어로 플로팅 카드를 정적 배치로 내렸습니다. 헤더도 ≤400px에서 6px 넘쳐 간격·로고 축소 분기를 추가했습니다.
  2. **직전 커밋에서 "모션 전면 제거"라고 보고했는데 페이지 스타일에 hover 이동이 4건 남아 있었습니다** — index `.prog-item`(`translateX(4px)`), family `.way`(`translateY(-5px)`), market `.shop`(`translateY(-4px)`), volunteer `.area`(`translateY(-4px)`). `styles.css` 만 훑고 페이지별 `<style>` 을 놓친 탓입니다. 색·보더·그림자 변화로 교체했습니다.
- **재발 방지**: `Claude_Memories/core/korean-line-break-rule.md` 를 `feedback` 메모리로 신설하고 `MEMORY.md` 에 색인했습니다. **원인은 이 규칙이 특정 프로젝트의 rules 파일에만 있어서 새 프로젝트로 넘어오지 않은 것**이므로, 프로젝트 경계를 넘는 메모리로 승격하는 것이 맞는 조치입니다.
- **검증**: `node tools/check-links.mjs` → 15페이지 이상 없음 / `verify-headless.mjs` → `총 52건 · 통과 52 · 실패 0` / `verify-a11y.mjs` → `총 23건 · 통과 23 · 실패 0`
- **커밋**: `1f0fab6`

## 2026-08-17 · 진입 게이트로 되돌아갈 수단 신설

- **지시**: "그 첫 도어페이지? 좌우 선택하는 페이지로 갈 수 있는 방법은 없어? 한번 선택하면 못 돌아가나?"
- **문제 확인**: 지적이 맞았습니다. 게이트 노출 조건이 `!storedSite()` 하나뿐이어서(`site.js`), 한 번 선택하면 **개발자도구로 `duri.site.v1` 을 지우는 것 말고는 다시 볼 방법이 없었습니다.** 상단 전환 바로 두 사이트를 오갈 수는 있었지만 선택 화면 자체에는 도달할 수 없었습니다. 제가 게이트를 만들 때 되돌아갈 경로를 설계하지 않은 누락입니다.
- **한 일**:
  - 전 페이지 푸터 맨 아래에 **"처음 선택 화면 다시 보기"** 링크(`.foot-reset` → `index.html?gate=1`). 최소 높이 44px, `#c6d8ce` + 밑줄(딥 배경 위 9.5:1)
  - 홈(`index.html`)에서는 **페이지 이동 없이 즉시** 게이트를 엽니다. 하위 페이지에서는 `index.html?gate=1` 로 이동
  - `?gate=1` / `#gate` 직접 진입 지원 — 북마크·공유 가능
  - **다시 열린 게이트에만** `.gate-close` 버튼 + ESC 닫기. 닫으면 **기존 선택이 그대로 유지**되고 초점이 푸터 링크로 복귀
  - 첫 방문 게이트에는 닫기 버튼을 두지 않음 — 두 선택지 자체가 진행 경로이므로
  - 선택 완료 후 `history.replaceState` 로 주소창의 `?gate=1` 정리(새로고침 시 재노출 방지)
  - 부수: `.foot-bottom` 글자색 `#7fa291`(5.02:1) → `#a9c4b5`(7.55:1) 로 상향
  - `tools/verify-headless.mjs` 에 **10건 추가**(링크 존재·터치 타깃·재노출·닫기·선택 유지·즉시 열림·주소 정리·첫 방문엔 닫기 없음·콘솔 에러)
- **결과**: ✅ 성공
- **사유**: `tools/verify-headless.mjs` **50건 전부 통과(실패 0)**, `verify-a11y.mjs` **23건 유지**. 실측값 — 푸터 링크 `index.html?gate=1` 높이 44px / `?gate=1` 진입 시 게이트 재노출되며 저장값 `coop` 유지 / 닫기 후 게이트 제거·`body.overflow` 해제·저장값 `coop` 유지 / 홈에서 클릭 시 URL 이 `/index.html` 그대로(이동 없음) / 선택 후 `location.search === ""` / 첫 방문 게이트 `닫기버튼=false`.
- **검증**: `node tools/check-links.mjs` → 15페이지 이상 없음 / `verify-headless.mjs` → `총 50건 · 통과 50 · 실패 0` / `verify-a11y.mjs` → `총 23건 · 통과 23 · 실패 0`
- **범위 조정**: 되돌아가기 링크를 **헤더가 아니라 푸터**에 뒀습니다. 상단은 이미 전환 바 + 헤더 + 계정 컨트롤로 밀도가 높고, 자주 쓰지 않는 유틸 링크는 국내 공공기관 사이트 관례상 푸터에 두는 편이 보수적입니다. 또 게이트를 다시 열 때 **저장된 선택을 지우지 않도록** 했습니다 — 닫기만 하고 나갈 수 있어야 하는데, 열자마자 값을 비우면 닫았을 때 갈 곳이 사라집니다.
- **커밋**: `4ca0561`

## 2026-08-17 · 시각 톤 절제 + 국내 웹 접근성(KWCAG 2.2) 준수

- **지시**: "홈페이지가 지나치게 화려하지 않았으면 좋겠고, 어쨌건 장애인 직업재활시설이니까 기본적으로 웹 접근성을 준수해서 만들어줘. 국내 웹 접근성을 기반으로 최대한 보수적이고 명확하게."
  확정 사항 — 시각 톤은 **장식(그라데이션·색면)은 유지하고 모션만 제거**, 파트너 로고 마퀴는 **정적 그리드로 교체**, 진입 게이트는 **단색·확대 없음**.
- **한 일**:
  - **모션 전면 제거** — 히어로 부동 블롭(`float`)·플로팅 카드(`bob`)·로고 마퀴(`logo-scroll`) 무한 애니메이션 삭제, `.reveal` 등장 애니메이션 무효화, 버튼·카드 hover `translateY` 제거(색·보더·그림자만 변화), 모달 등장을 불투명도만으로, 메가메뉴 슬라이드 제거
  - **파트너 로고**: 마퀴 → `.logo-grid` 정적 `<ul>`(6/3/2열). 매 방문 셔플도 제거(낭독 사용자가 위치를 기억할 수 없음)
  - **진입 게이트**: 그라데이션·점 패턴·hover 확대·페이드 인 제거 → 단색 `#1f6242` / `#17567b` + 3px 흰 구분선. `role="dialog"` + `aria-modal` + 첫 선택지 초점 + Tab 갇힘
  - **명도 대비(5.3.1)**: `--muted` 4.04→5.97, 전환 바 비활성 3.49→9.4(`#d7e2dc`), 앰버 글자 2.42→5.22(`--amber-700` 신설), `org` 역할 텍스트 3.95→4.79, rehab `--muted` 4.23→5.85. **팔레트는 그대로 두고** `--brand` 를 '면 색'으로 한정하고 글자는 `--brand-deep` 으로 옮겨 해결
  - **레이블(7.4.1)**: `label for` ↔ 컨트롤 `id` **21개 결속**(기존 0개). 필수는 `required` + `aria-required` + `*` + `<span class="sr-only">(필수 항목)</span>` 3중. 폼에 `novalidate`
  - **건너뛰기(6.4.1)**: `site.js` 가 "본문 바로가기"를 `body` 최상단에 주입, 15페이지 전부 `<main id="main">` 신설
  - **대체 텍스트(5.1.1)**: 장식 SVG **48개** + ICON 세트에 `aria-hidden="true" focusable="false"`
  - **키보드(6.1.1)**: 갤러리 타일 12개 `role="button" tabindex="0"` + `aria-label` + Enter/Space
  - **조작 가능(6.1.3)**: 탭·필터·금액·페이저·닫기 버튼을 44×44 이상으로
  - **색 무관 인식(5.2.1)**: `.seg`/`.amt-row`/`.filter-row`/`.tabs` 를 `role="radiogroup"` + `role="radio"` + `aria-checked` + 화살표 키 이동. 페이저 `aria-current`. 게시판 고정글에 "공지 고정" 낭독 텍스트. `.link` 에 밑줄
  - **오류 정정(7.4.2)**: `role="alert"` + **어느 항목인지 이름을 넣은** 문구 + `aria-invalid`(2px 보더) + 초점 이동
  - **대화상자(8.2.1)**: `wireModal()` 이 `role="dialog"`/`aria-modal`/`aria-labelledby` 부여, 열 때 초점 진입, Tab 갇힘, ESC 닫힘, 닫을 때 원래 위치 복귀
  - **구조**: 모바일 메뉴 `<nav>` + 버거 `aria-expanded`/`aria-controls`, 푸터 제목 `h4`→`h2`(제목 단계 건너뜀 방지), 푸터 `aria-label`
  - `tools/verify-a11y.mjs` **신설**(23건). `rules/00-core.md` 에 접근성을 절대 규칙 7·8 + 1-1절 표로 승격, `20-design.md`·`40-verify.md`·`50-roadmap.md` 갱신
- **결과**: ✅ 성공
- **사유**: `tools/verify-a11y.mjs` **23건 전부 통과(실패 0)**, `tools/verify-headless.mjs` **40건 전부 통과** = 합계 63건. 근거가 되는 실측값 — 명도 대비는 15페이지 텍스트를 전수 순회하며 반투명 배경을 합성해 계산한 결과 위반 0건 / 무한 반복 애니메이션 15페이지 0건 / 결속 없는 입력 0건 / 이름 없는 링크·버튼 0건 / 44×44 미달 컨트롤 0건 / 중복 id 0건 / 콘솔 에러 0건 / 오류 문구 실제값 `'성함 / 단체명' 항목을 입력해 주세요.` + `aria-invalid=true` + 보더 2px + 초점 이동 확인 / 로그인 대화상자 초점 진입·ESC·복귀 전부 true / 게이트 720·720.
- **검증 중 발견해 고친 별개 버그 2건**:
  1. **`.cta-band` / `.cta-cards` / `.cta-tile` 이 어디에도 정의돼 있지 않았습니다.** index·family 의 CTA 섹션이 흰 글자를 흰 배경에 렌더하고 있어 **텍스트가 사실상 보이지 않는 상태**였습니다. 대비 검사가 `1.00:1` 로 잡아냈습니다. 단색 딥 그린 면으로 정의(흰 글자 7.3:1). 사용되지만 정의되지 않은 클래스를 전수 조사해 확인했습니다.
  2. **`family.html` 의 `.acct` 규칙이 헤더 계정 컨트롤(`#acct`)까지 덮어쓰고 있었습니다.** 페이지 `<style>` 이 `styles.css` 뒤에 오므로 후원 계좌 박스용 `background:var(--green-800); padding:30px` 가 헤더 로그인 영역에 적용됩니다. `.donate-acct` 로 분리.
- **범위 조정**:
  - `.reveal` 클래스와 `data-delay` 를 15개 HTML 에서 걷어내는 대신 **CSS 를 무효화**했습니다. 클래스 제거는 큰 diff에 파손 위험만 있고 사용자에게 이득이 없습니다. 남은 machinery 는 `20-design.md` 에 "시각 효과 없음 · 되살리지 말 것"으로 명시했습니다.
  - `--brand`(#2a8159)를 **어둡게 바꾸지 않았습니다.** 브랜드 색을 유지하면서 대비를 맞추려고, `--brand` 를 면 색으로 한정하고 글자를 이미 존재하던 `--brand-deep` 으로 옮기는 방식을 택했습니다.
  - 지시에 없던 `tools/verify-a11y.mjs` 를 만들었습니다. "접근성을 준수한다"는 검증 수단 없이는 다음 작업에서 곧바로 무너집니다. 검사 스크립트를 커밋 전 필수 관문으로 문서화했습니다.
  - 그라데이션·이미지 배경 위 텍스트 **55종은 자동 판정이 불가**하여 통과로 세지 않고 "사람이 확인" 목록(`50-roadmap.md` B10~B13)으로 남겼습니다. 이걸 통과로 처리하면 검증이 거짓이 됩니다.
- **커밋**: `01d0795`

## 2026-08-17 · HANDOFF 문서 신설 — 지시를 받으면 무조건 여기부터

- **지시**: "rules에 hand-off 문서를 만들어서, 제일 처음 일을 시키면 무조건 hand-off 문서에 일을 남기는 게 좋아 보여. 그리고 무조건 일을 시키면 그 문서부터 확인할 수 있게 만들어줘."
- **한 일**:
  - **[HANDOFF.md](HANDOFF.md) 신설** — 진행 중/보류 작업, 지시 등록 틀, 상태 표기(`▶ 진행 중` / `⏸ 보류` / `✅ 종료`), "최근 종료" 3건 유지 규칙
  - **HANDOFF = 진행 중 / WORKLOG = 끝난 것** 으로 역할을 명시 분리
  - 진입 경로 4곳을 전부 "HANDOFF 먼저"로 갱신:
    `CLAUDE.md` 제1규칙과 파일 지도 표 · `rules/README.md` 읽는 순서 ①·`rules/00-core.md` 표준 작업 흐름(9단계로 확장, ①은 조사·수정보다 먼저) · `.claude/settings.json` SessionStart 훅 문구
- **결과**: ✅ 성공
- **사유**: 세션 시작 훅과 `CLAUDE.md`·`rules/README.md`·`rules/00-core.md` 네 경로 모두가 HANDOFF를 첫 단계로 지시하므로, 어느 경로로 진입해도 이 문서를 먼저 만나게 됨. 작업 흐름에 "지시를 받았는데 HANDOFF가 비어 있는 상태로 코드를 열지 말 것"을 명문화해 등록 누락 시 규칙 위반으로 판정 가능해짐.
- **검증**: `node tools/check-links.mjs` → 이상 없음 / `rules/` 상호 링크 전수 확인(HANDOFF ↔ README ↔ WORKLOG ↔ 50-roadmap)
- **범위 조정**: 지시는 "문서 신설 + 먼저 확인"이었으나 **WORKLOG와의 역할 경계를 명시**했습니다. 둘 다 "작업 기록"이어서 경계가 없으면 같은 내용이 두 곳에 중복되거나 어느 쪽도 갱신되지 않게 되기 때문입니다.
- **커밋**: `d6ee879`

---

## 2026-08-17 · 첫 방문 진입 게이트 + 두 갈래 사이트 전환 바

- **지시**: "이 사이트에 처음 접속하면 전체 화면을 꽉 채우는 형태로 좌측 50%는 두리손잡고사회적협동조합 바로가기, 우측 50%는 두리손잡고 직업재활센터 바로가기로 구분. 어느 쪽을 선택하면 홈페이지 제일 상단 5%를 상단 고정으로 해서 두 사이트를 구분해 갈 수 있는 메뉴를 신설."
  추가 확정 사항 — 현재 페이지는 조합 사이트, 우측은 **준비중 페이지**로 연결, 게이트는 **첫 방문에만**(선택 기억), 우측 명칭은 **두리손잡고 직업재활센터**, 우측 키 컬러는 **오션 블루 `#1f6f9e`**.
- **한 일**:
  - 전체화면 진입 게이트 `.gate` 주입 — 좌 50% 조합(그린) / 우 50% 직업재활센터(오션 블루), hover 시 해당 면 확대, ≤760px에서 상하 분할 (`assets/site.js`, `assets/styles.css`)
  - 선택을 `localStorage["duri.site.v1"]`(`coop`/`rehab`)에 저장. 재방문 시 게이트 미표시
  - 전 페이지 최상단 고정 전환 바 `.site-switch` 신설 — 높이 `clamp(38px,5vh,52px)`, 현재 갈래에 `.on`. 헤더 sticky `top`을 바 높이만큼 내림
  - `<html data-site="rehab">` 스코프로 컬러 토큰 전체를 오션 블루 스케일로 재정의 → 기존 컴포넌트가 그대로 새 색으로 렌더 (`assets/styles.css`)
  - 직업재활센터 준비중 페이지 `duri-website/rehab.html` 신설. `site.js`가 이 갈래에서는 조합 NAV·후원 버튼·조합 푸터 링크를 주입하지 않고 간략 푸터만 붙임
  - favicon을 갈래 색에 맞춰 주입
- **결과**: ✅ 성공
- **사유**: `tools/verify-headless.mjs` **40건 전부 통과(실패 0)**. 근거가 되는 실측값 —
  게이트 좌우 폭 **720 / 720 @뷰포트 1440** (정확히 50/50) · 전체화면 높이 충족 · 좌측 이름에 "사회적협동조합", 우측 `href="rehab.html"` · 진입 시 `activeElement=gate-half coop`(키보드 접근 가능) ·
  조합 선택 후 게이트 DOM 제거 + `duri.site.v1="coop"` + `body.overflow` 해제 + **재방문 시 미노출** ·
  우측 선택 시 `/rehab.html` 이동 + `duri.site.v1="rehab"` + 프라이머리 버튼 실렌더색 **`rgb(31,111,158)`** + 푸터 **`rgb(13,44,64)`** + 조합 메뉴·후원 버튼 미주입 ·
  전환 바 6개 페이지 전부 존재, 높이 **45px**(=5vh @900), `body` 최상단 요소, `headerTop=45`로 겹침 없음, 항목 폭 **720/720**, 활성 1개 + `aria-current=1` ·
  모바일 390px에서 상하 **390/390** 분할 · 게시판·폼 회귀 통과 · **전 구간 콘솔 에러 0**.
- **검증**: `node tools/check-links.mjs` → `✅ 이상 없음 — 페이지 15개 검사` / `node tools/verify-headless.mjs` → `총 40건 · 통과 40 · 실패 0` / `gh run list` → success
- **검증 중 발견해 수정한 것**: 첫 실행에서 게이트 좌우 폭이 **758 / 682** 로 어긋났습니다. 원인은 `.gate-half:focus-visible{flex-grow:1.12}` — 진입 시 첫 선택지에 자동 포커스를 주는데 Chrome이 이를 `:focus-visible`로 판정해 좌측만 12% 넓어진 것입니다. 사용자가 명시한 "좌우 50%"가 깨지므로 **확대를 `:hover` 에서만** 일어나도록 고쳤고(포커스 표시는 outline으로 유지), 재검증에서 720/720이 됐습니다.
- **범위 조정**: "5%"를 고정 `5vh`로 쓰면 작은 노트북에서 30px 미만이 되어 텍스트가 잘리므로 `clamp(38px, 5vh, 52px)`로 하한·상한을 뒀습니다(1440×900에서 실측 45px). 재방문자가 `rehab`을 저장한 상태로 `index.html`에 와도 **강제 리다이렉트하지 않았습니다** — 북마크·검색 유입을 가로채면 조합 페이지를 볼 방법이 없어지기 때문이며, 이동 수단은 상단 전환 바로 남겨뒀습니다.
- **커밋**: `d6ee879`

---

## 2026-08-17 · 디자인 훅 지적 2건 검토 (오탐 판정) + 문서 오기 수정

- **지시**: (자동 훅) `styles.css` L256·L473 `side-tab` — "카드 한쪽 변의 두꺼운 컬러 보더는 AI 티가 나는 패턴".
- **한 일**:
  - **둘 다 오탐으로 판정, CSS 무수정.**
    L256 `.tl-items{border-left:2px solid var(--line-strong)}` = 연혁 타임라인의 세로 연결선(`.tl-item::before` 점들이 이 선에 얹힘). 카드 액센트가 아니고 색도 중성 토큰.
    L473 `.site-switch .sw-item.on::after` = `left:0;right:0;bottom:0` 로 **하단 전체 폭**을 채우는 탭 활성 표시자. 한쪽 변 보더가 아님.
  - `rules/20-design.md` 전환 바 절의 **오기 수정** — "좌하단에 3px 하이라이트 바"로 잘못 적어둔 것을 "하단 전체 폭 3px 앰버 하이라이트"로 정정하고, 색각 이상 대비용 형태 단서라 제거 금지임을 명시
- **결과**: ✅ 성공
- **사유**: 두 셀렉터의 실제 좌표값(`border-left` 는 타임라인 컨테이너, `::after` 는 `left:0;right:0`)이 훅의 판정 근거(카드 한쪽 변)와 맞지 않음을 코드로 확인. 억제 규칙은 넣지 않았음 — 오탐 판단은 작업자 것이고 영구 예외는 사용자 확인 후 넣는 것이 옳다고 봄.
- **범위 조정**: 훅은 CSS만 지적했으나 검토 중 **규칙 문서가 코드와 어긋난 것**을 발견해 같이 고쳤습니다. `20-design.md` 서두가 "코드가 정답이며 변경 시 둘 다 맞춘다"고 규정하고 있어 방치할 수 없습니다.
- **커밋**: `a01c740`

---

## 2026-08-17 · 접근성 버그 2건 수정 (select 포커스 링 · 키보드 메가메뉴)

- **지시**: 전수 점검에서 나온 버그 2건을 이번 작업에 같이 포함할 것.
- **한 일**:
  - `assets/styles.css` — `.field input:focus,.field select,.field textarea:focus` 에서 `select`에 빠져 있던 `:focus`를 보완. 모든 셀렉트가 항상 4px 포커스 링을 켜고 있던 오타 버그
  - `assets/styles.css` — `.nav-item:hover .mega` 에 `:focus-within`을 추가해 키보드로 메가메뉴 진입 가능하게
  - `assets/styles.css` — 전역 `:focus-visible` 표시 스타일 추가
- **결과**: ✅ 성공
- **사유**: 수정 전에는 셀렉트가 항상 포커스 링 상태였고 키보드로 메가메뉴가 열리지 않았음. 수정 후 실측 — 셀렉트 기본 `box-shadow="none"`, 포커스 시에만 링 표시 / 메가메뉴 `visibility` 가 포커스 전 `hidden` → 상단 링크 포커스 `visible` → 내부 링크 포커스 `visible` / `.nav-top` 포커스 시 `outline-width=3px`.
- **검증**: `tools/verify-headless.mjs` 의 "버그A/버그B/포커스 표시" 5건 전부 통과. `getComputedStyle` 실측값 기준. ⚠️ `visibility` 는 `transition` 대상이라 **트랜지션(0.22s)이 끝난 뒤에 읽어야** 합니다 — 즉시 읽으면 여전히 `hidden`으로 나옵니다(첫 검증에서 이 때문에 오탐).
- **범위 조정**: 지시는 버그 2건이었으나 **전역 `:focus-visible` 스타일을 함께 넣었습니다.** `:focus-within`으로 메뉴가 열려도 포커스 위치를 볼 수 없으면 수정이 반쪽이 되기 때문입니다(사이트 전체에 포커스 표시가 전무한 상태였음).
- **커밋**: `d6ee879`

---

## 2026-08-17 · 규칙 폴더 재구성 — 루트는 CLAUDE.md 하나, 규칙은 rules/

- **지시**: "① 폴더에 rules를 만들고 외부에는 CLAUDE.md만 둔다. ② CLAUDE.md는 무조건 rules를 참조하도록 규칙을 신설하고, rules에는 개발을 위한 모든 규칙을 넣는다(디자인 포함). ③ rules 내에 모든 지시사항·개선사항을 볼 수 있는 파일을 신설하고, 성공이면 성공 사유·실패하면 실패 사유를 지정해 기록한다."
- **한 일**:
  - `rules/` 신설. 루트 문서를 `CLAUDE.md` 하나로 축소 — 규칙 본문을 전부 걷어내고 **rules/ 참조 의무 + 파일 지도**만 남김
  - `DESIGN.md` → `rules/20-design.md` **git mv**(이력 보존). 사이트별 테마·게이트·전환 바 컴포넌트 절 추가
  - `rules/README.md`(인덱스) · `00-core.md`(절대 규칙·작업 흐름·두 갈래 구조·인증) · `10-structure.md`(폴더·site.js/board.js·새 페이지) · `30-content.md`(메뉴·공지·갤러리·로고·연락처) · `40-verify.md`(검증·배포) · `50-roadmap.md`(점검 결과) 작성
  - **`rules/WORKLOG.md` 신설** — 이 파일. 지시/한 일/결과/사유/검증/범위 조정/커밋 필드
  - `tools/check-links.mjs` 신설 — 없는 파일·앵커·리소스, 절대경로, `img alt` 누락 검사
  - `tools/verify-headless.mjs` 신설 — 게이트·전환 바·테마·접근성·게시판·폼을 40건 자동 검사
  - `.claude/settings.json` SessionStart 훅 문구를 `rules/` 참조 지시로 교체
  - 죽은 CSS 제거 — `html[data-palette="forest"|"sage"]` 2줄(참조하는 코드 없음, 절대 규칙 4가 금지한 "디자인 옵션" 잔재)
- **결과**: ✅ 성공
- **사유**: 루트에 `CLAUDE.md` 외 문서가 남아 있지 않음(`ls *.md` = CLAUDE.md 하나)을 확인. `rules/` 인덱스의 모든 링크가 실제 파일로 연결되며, `DESIGN.md`가 git 이력을 유지한 채 이동됨(`git log --follow`). 지시 3항의 기록 파일이 이번 세션 작업 3건을 첫 항목으로 담고 있음.
- **검증**: `ls *.md` → `CLAUDE.md` / `git status --short` → `R DESIGN.md -> rules/20-design.md` / `node tools/check-links.mjs` → 이상 없음
- **범위 조정**: 지시에 없던 `tools/` 검증 스크립트 2개를 추가했습니다. `40-verify.md`에 "검증하라"고만 쓰면 실행할 수단이 없고(이 기기에 `puppeteer-core`도 미설치였음), 규칙 문서가 실행 가능해야 지켜지기 때문입니다. 개발용이며 배포 산출물(`duri-website/`) 밖에 있어 사이트에는 포함되지 않습니다. 죽은 `data-palette` CSS 제거도 지시 범위 밖이지만, 새로 도입한 `data-site` 테마 전환과 혼동될 수 있어 함께 정리했습니다.
- **커밋**: `d6ee879`

---

## 2026-08-17 · 개발 상태 전수 점검

- **지시**: "홈페이지의 전반적인 기능 개선을 진행할 건데 (…) 현재 개발 상태를 점검해봐 먼저."
- **한 일**: HTML 14개 + 에셋 3개(3,123줄) 전수 점검. 버그 2건, 접근성 6건, SEO 6건, 성능 3건, 기능 미완성 4건, 인프라 3건을 분류해 [50-roadmap.md](50-roadmap.md)에 기록. 링크·앵커·리소스 정합성은 임시 스크립트로 전수 검사.
- **결과**: ✅ 성공
- **사유**: 점검 결과가 추측이 아니라 파일:줄 단위 근거를 갖고 있음(예: `styles.css:268` 셀렉트 `:focus` 누락, `styles.css:322` `:hover` 전용 메가메뉴). 링크 검사는 스크립트 출력으로 통과 확인.
- **검증**: 임시 Node 스크립트로 14페이지 href/앵커/src 전수 검사 → 이상 없음 / `gh run list` → 최근 5회 배포 전부 success
- **커밋**: (코드 변경 없음)

---

## 그 이전 (커밋 이력 요약 — 이 파일 신설 전)

| 커밋 | 내용 |
|---|---|
| `13e238c` | docs: 가이드 2분할 — CLAUDE.md(작업) + DESIGN.md(디자인) |
| `3a8c142` | 헤더 로그인 시 레이아웃 정리 + 파트너 로고 마퀴(무한 가로 스크롤) |
| `d121495` | docs: 표준 작업 흐름(변경 후 자동 커밋·푸시·배포) 규칙 추가 |
| `7da41c4` | 게시판 글쓰기·관리자 로그인 + 전 페이지 버튼 동작 + 작업 가이드 |
| `76bbc8e` | ci: Pages 워크플로 자동 활성화(enablement) 설정 |
| `4931282` | 두리손잡고 정적 홈페이지 + GitHub Pages 배포 워크플로 |

---
© 사회적협동조합 두리손잡고
